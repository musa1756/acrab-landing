.DS_Store

# Исходные скриншоты и записи экрана, из которых собраны assets/about/*.webp.
# Timeweb отдаёт репозиторий как есть, без сборки, поэтому всё закоммиченное
# сразу становится публичным — сырьё держим локально.
Main/
